pipeline {
  environment {
    HOME="."
  }
  stages {
    stage('Build') {
      steps {
        bat "echo 'buid'"
      }
    }
    stage('Test') {
        steps {
          bat "echo 'test'"
        }
    }
    stage('Deploy') {
        steps {
          bat "echo 'deploy'"
        }
    }
  }
}
